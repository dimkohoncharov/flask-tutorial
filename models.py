# from app import db

# class User(db.Model):
#     id = db.Column(db.Integer, primary_key=True, unique=True, nullable=False)
#     email = db.Column(db.Strin, unique=True, nullable=False)
#     # password = db.Column(db.String, nullable=False)